# Local dev: leave VITE_API_URL unset. Vite proxies /api -> http://localhost:8000.
# Production (Vercel): set VITE_API_URL to the Render backend origin, no trailing slash.
#   VITE_API_URL=https://passkey-api.onrender.com
